global:
  message: Welcome to customer service dev environment!!

server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/customer_master
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
